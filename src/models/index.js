const productModel = require('./product.model');
const salesModel = require('./sales.model');
const deleteProductModel = require('./product.model');

module.exports = {
  productModel,
  salesModel,
  deleteProductModel,
};
