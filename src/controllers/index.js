const productController = require('./product.controller');
const salesController = require('./sales.controller');
const deleteProductController = require('./product.controller');

module.exports = {
  productController,
  salesController,
  deleteProductController,
};
